package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Vault Palette
val SlatePrimary = Color(0xFF0F172A) // Rich slate black (Dominant text, headers, authority)
val TealAccent = Color(0xFF0F766E) // Forest Teal (Assets, verified nominee, active, insurance)
val CreamGold = Color(0xFFB45309) // Warm Amber Gold (Dues, alerts, premium highlights, emergency triggers)

val SlateLightBg = Color(0xFFF8FAFC) // Slate-50 Clean off-white background
val SlateBorder = Color(0xFFE2E8F0) // Subtle outline border Slate-200
val RedAlert = Color(0xFFDC2626) // Bright crimson for missing documents, revoked access, and immediate warnings
val GreenSuccess = Color(0xFF16A34A) // Stable emerald for verified statuses and completed claims
